#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <locale.h>
#include <limits.h>

#define TAM 5

int main() {
    setlocale(LC_ALL, "");
    
    int vetor [TAM];
    int i;
    
    printf("Digite os elementos do vetor:\n");
    for (i = 0; i < TAM; i++) {
	
    	printf("Elemento %d: ", i + 1);
    	scanf("%d", &vetor[i]);
	}
	
	return 0;
	
}
